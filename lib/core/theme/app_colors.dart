import 'package:flutter/material.dart';

abstract class AppColors {
  // --- Brand: Gold (primary) ---
  static const gold = Color(0xFFE8B54A);
  static const goldDeep = Color(0xFFD4A03A);
  static const goldLight = Color(0xFFF1C878);
  static const goldSoft = Color(0xFFFBE9C0);

  // --- Brand: Maroon (secondary) ---
  static const maroon = Color(0xFF5B1E0F);
  static const maroonDeep = Color(0xFF3E140A);
  static const brown = Color(0xFF7A3A1D);
  static const brownSoft = Color(0xFFA8694A);

  // --- Neutral: Paper ---
  static const paper = Color(0xFFF7F4ED);
  static const paper2 = Color(0xFFEFEAE0);
  static const cream = Color(0xFFFBF8F1);
  static const white = Color(0xFFFFFFFF);
  static const ink = Color(0xFF2A1810);
  static const mute = Color(0xFF8A7768);
  static const line = Color(0xFFE7DFD0);

  // --- Accent ---
  static const violet = Color(0xFFB084E8);
  static const violetDeep = Color(0xFF7A52C2);
  static const carrot = Color(0xFFF27B25);
  static const leaf = Color(0xFF6FBE44);
  static const sunshine = Color(0xFFF7D038);
  static const pink = Color(0xFFE766B6);
  static const blue = Color(0xFF2E6BE8);

  // --- Semantic ---
  static const success = Color(0xFF3FA463);
  static const warning = Color(0xFFE8B54A);
  static const danger = Color(0xFFC43B2B);
  static const info = Color(0xFF2E6BE8);

  // --- Semantic surfaces ---
  static const successSurface = Color(0xFFE3F3D6);
  static const warningSurface = Color(0xFFFBE9C0);
  static const dangerSurface = Color(0xFFFDE8E6);
  static const infoSurface = Color(0xFFE3F7EC);
  static const violetSurface = Color(0xFFF0E6FD);

  // --- Semantic on-surface text ---
  static const successText = Color(0xFF2F7A2A);
  static const dangerText = Color(0xFFC43B2B);
  static const violetText = Color(0xFF7A52C2);

  // --- Background ---
  static const background = Color(0xFF111111);
}
